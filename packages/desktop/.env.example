# HermesHQ server URL — baked into the build so users don't need to configure anything.
# Set this before running `npm run build` or `electron-builder`.
# Example: https://hermeshq.gcaplabs.com
VITE_HERMESHQ_URL=
